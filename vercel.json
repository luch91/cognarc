{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @cognarc/dashboard build",
  "outputDirectory": "apps/dashboard/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
